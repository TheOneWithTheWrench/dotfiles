-- ts_ls is now configured in lsp-config.lua using the new vim.lsp.config API
-- The configuration is in ~/.config/nvim/lsp/ts_ls.lua
-- This file can be deleted if no other JavaScript-specific plugins are needed
return {}
